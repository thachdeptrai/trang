// Public browser configuration for the shared wish wall.
// IMPORTANT: use ONLY a Supabase publishable/anon key here.
// Never put service_role or secret keys in a GitHub Pages repository.
window.TRANG_SUPABASE = Object.freeze({
  url: 'YOUR_SUPABASE_PROJECT_URL',
  key: 'YOUR_SUPABASE_PUBLISHABLE_KEY'
});
